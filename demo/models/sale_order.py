from odoo import models, fields, api


class ResPartnerscdu(models.Model):
    _inherit = 'sale.order'

    def cron_demo(self):
        print("?????????????????????????????????????????????")
        for rec in self:
            if rec.state == "draft":
                rec.state = "sent"
