<odoo>
    <data>
        <!-- server action to the one above -->

        <record model="ir.actions.server" id="res_partner_action_server">
            <field name="name">New Record </field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="binding_model_id" ref="model_res_partner"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
                action = record.record_cree()
            </field>
        </record>

        <record model="ir.actions.server" id="res_partner_action_server_2">
            <field name="name">Record </field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="binding_model_id" ref="model_res_partner"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
                action = record.over_wirte()
            </field>
        </record>
    </data>
</odoo>